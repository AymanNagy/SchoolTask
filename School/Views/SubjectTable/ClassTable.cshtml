@model School.Models.ViewModel.ClassTableViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}



    <table class="table table-hover text-nowrap">
        <thead>
            <tr>
                <th>Saturday</th>
                <th>Sunday</th>
                <th>Monday</th>
                <th>Tuesday</th>
                <th>Wednesday</th>
                <th>Thursday</th>
                <th>Friday</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                @foreach (var item in Model.ListOfSubSat)
                {
                    <td>
                        @item.TeachersAndSubject.teacher.TeacherName
                        <br />
                        @item.TeachersAndSubject.Subject.SubjectName
                    </td>
                }
            </tr>
            <tr>
                @foreach (var item in Model.ListOfSubSun)
                {
                    <td>
                        @item.TeachersAndSubject.teacher.TeacherName
                        <br />
                        @item.TeachersAndSubject.Subject.SubjectName
                    </td>
                }
            </tr>
            <tr>
                @foreach (var item in Model.ListOfSubMon)
                {
                    <td>
                        @item.TeachersAndSubject.teacher.TeacherName
                        <br />
                        @item.TeachersAndSubject.Subject.SubjectName
                    </td>
                }
            </tr>
            <tr>
                @foreach (var item in Model.ListOfSubTue)
                {
                    <td>
                        @item.TeachersAndSubject.teacher.TeacherName
                        <br />
                        @item.TeachersAndSubject.Subject.SubjectName
                    </td>
                }
            </tr>
            <tr>
                @foreach (var item in Model.ListOfSubWed)
                {
                    <td>
                        @item.TeachersAndSubject.teacher.TeacherName
                        <br />
                        @item.TeachersAndSubject.Subject.SubjectName
                    </td>
                }
            </tr>
            <tr>
                @foreach (var item in Model.ListOfSubThr)
                {
                    <td>
                        @item.TeachersAndSubject.teacher.TeacherName
                        <br />
                        @item.TeachersAndSubject.Subject.SubjectName
                    </td>
                }
            </tr>
            <tr>
                @foreach (var item in Model.ListOfSubFri)
                {
                    <td>
                        @item.TeachersAndSubject.teacher.TeacherName
                        <br />
                        @item.TeachersAndSubject.Subject.SubjectName
                    </td>
                }
            </tr>

        </tbody>
    </table>
<p>To Print Press CTRL+P</p>



